# 20250219 업무일지


## TMS 이슈



## 1. OMS & Ai 역매칭 프로세스 수정

### 개요
> 1. OMS ( Z20 ) 저장시 -> tms.tb_get_ai_chapter_hist_serno 테이블에 시험지 테이블에 적재
> 2. Admin-Client에 엑셀다운로드 위치 표시 -> 바탕화면에 ReversMatching 폴더명 생성 및 다운로드
> 3. 엑셀 결과표시 빈값 표시 
> 4. 엑셀에 개요번호 맨 앞에 표시
> 5. 기존엔 예측 타입이 존재하지 않으면 보여주지 않았는데, 현재 예측 타입이 존재하지 않더라도 리스트에 노출


- [x] 처리완료




## 2. 문항 동기화 (extract-question)

### 개요
> 그룹 타입 G(수준별) 동기화 배치 추가
> 기존 그룹들과 같이 돌아가다보니 G 그룹의 데이터가 호출되지 않아
> 호출 그룹의 분리 필요


### 처리내역
> 호출 배치 작업 완료, 배포 대기중

- [x] 처리완료



## 3. AI 추출 팁 html 옛한글 깨짐 현상
### 개요
> AI로 생성한 html tip 데이터 옛한글 깨짐 현상
> 인코딩 문제인지, 폰트 문제인지 확신할 순 없으나 깨짐
> 테스트 필요








## 4. AnalysisTag.cs 하드코딩 제거

### 개요
> 현재 AnalysisTag.cs 에서 하드코딩으로 출판사 데이터를 검사하고 있는데 이전 수능문항 내신 DB 저장 오류와 같이
> 데이터가 동기화 되지 않는 경우 







## 5. Ai 수준별 생성 후 시험지 매칭 처리

### 개요
> Ai 수준별 생성 후 새로운 시험지 번호가 부여되는데 이 때 examPaper에 데이터가 쌓여서 정상적으로 처리되는지 확인 필요


### 처리내역
> 현재 수준별 생성 후 새로운 시험지 번호의 examPaper 데이터 동기화 처리 완료


- [ ] 처리완료





## 6. 특정 출판사 미노출  처리

### 개요
> 22개정 중1 국어 천재(노미숙)1-2 출판사 현재 클라우드에서 노출되고 있는데,
> 미노출 처리. (2학기 교과서라 아직 노출되면 안 됨)



### 처리내역
> 현호형이랑 협의해서 프로시저에서 해당 subjt_id(4733) 미노출로 변경처리 하였음 
> 추후 노출이 필요할 경우 프로시저 수정이 필요함


- [x] 처리완료





## 7. 문항 동기화

### 개요
> 문항 동기화가 처리되지 않아 데이터를 수동 동기화 처리

- [x] 처리완료



## 8. 수학 레이텍 -> 한글 수식 처리 오류









## 9. DB 저장 / 내신미주 

### 개요
> 1. DB 저장시 통합사회1,2의 단원코드 검사 에러가 발생하여 저장되지 않음
> 2. 내신미주 출판사를 통한 검색시 특정 내신미주 출판사는 검색되지 않음



### 처리내역
> 1. 기존에 codes[0]에서 1,2로 분류하여 publishName값을 반환할 때 고정으로 courseName + 1 / courseName + 2로 반환하고 있엇으나, 해당 로직을 publishName + 1 / publishName + 2 로 반환하는 편의 메소드 작성하여 단원코드 마지막 1,2 문제를 해결함.
> 2. ?? : 현재까지 파악하기로는 tqq_textbook_question_conn에 left join할 때 연결되는 데이터가 존재하지 않아 발생하는 문제, 하지만 해당 테이블에 데이터를 입력하는 부분은 문제보기(PreView에서 문항을 수정할 때 tqq_textbook_question_conn에 정보가 입력됨.) insertTextbookQuestionConnect로 입력되는데 22개정은 문제보기(PreView)에서 문항을 수정하지 않아 연결 데이터가 없어 내신미주 출판사로 qstId를 추출할 수 없어 현재 문제가 발생하고 있는것으로 보여짐.









## 10. 유사문항 추출시 문항에 기출 문항 제거

### 개요
> 대표님이 지시 사항으로 처리
> 유사문항 추출시 선행/후행 문항과 더불어 기출 문항이 나옴
> 이 때 기출 문항을 사람이 보고 판단해서 선택을 하지 않고 있음
> 그러므로 기출 문항을 유사 문항에 나올 때 데이터를 반환하지 않는 로직 수정이 필요함.









## 11. 문항 동기화 싱크 테이블

### 개요
> 1. tqq_question_sync에 신규 문항을 넣어서 동기화를 처리
> 2. chat gpt가 문항을 html로 뱉는다.
> 3. html을 hwp로 변환
> 4. qstId를 생성해서
> 5. tqq_question_sync에 삽입해줘야 됨

### 유의사항
> 전체 배치는 하루에 한번 돌아가는데, 이거 말고도 몇분에 한번씩 문항을 동기화 하는 작업이 필요함.














## Bigquery 이슈

## 1. APP mo-web 내재화 작업 관련 트래킹 컬럼 추가 요청

### 개요
APP mo-web 내재화 작업 관련 빅쿼리 컬럼 추가가 필요해 메일 드립니다.

| No  | 컬럼명               | 컬럼타입         | NULL | 이름    |
| --- | ----------------- | ------------ | ---- | ----- |
| 1   | SUBJECT_DETAIL_CD | VARCHAR(100) | NULL | 상세 과목 |

### 처리내역
> 




## 기타이슈

## RM 인수인계 전 ci / cd 구축 확인의 건

### 개요

1. 유선상으로 문의드렸던 CI/CD 관련된 인프라 구성 확인 요청드립니다.
   ㄴ git : gitHub Enterprise (AWS) 구축
   ㄴ jenkins : 2.176.1
2. CI/CD 관련 내용은 프로젝트 진행에서 없던 요건 부분인데,
   추가 작업에 대한 공수, 예산(무상/유상) 등에 대한 검토가 필요할 것으로 판단됩니다.
3. 소스 패키징 되어있는 리스트는 작성해서 교육 전 공유드리도록 하겠습니다.
4. 기술이전 교육 일정
    - 차주 수요일 / 목요일 가능하신 일정 있으신지 확인부탁드립니다.
    - java, 전자정부프레임워크에 대한 기반 부분은 제외하고 진행

  










## 보류 / 대기 / 잔존이슈

## 1. 수능 2022년 개정 이투스 안내

### 개요
수능 2022년 개정이 시작되면서 이투스가 수능 개정이 시작되면서 이투스의 단코 입력이 필요한 상황
수능 2022개정 데이터 정리해서 김한길 담당자한테 안내

### 처리 내역
> 2022년 단코를 입력함에 따라 필요한 사전 로우 데이터
> 1. 교육과정
> 2. 학년
> 3. 과목
> 4. 출판사
> 5. 대단원
> 6. 중단원
> 7. 소단원
> 8. 내용토픽
> 9. 형식토픽

데이터를 수능 2015개정에 따라 입력 처리



## 2. 족보닷컴 어드민 시험지 변경 감지 프로세스 개발

### 개요
> 시험지 변경(오답, 수정 등)의 이유로 기존에 extract-question서비스에서 시험지 동기화가 완료된 시험지들의
> hwp model에서 한번 더 시험지 동기화 시스템 추가 개발

### 조건

> 1. 시험지가 등록된 상태에서 변경 처리(hwp 파일이 변경 되었을 때)
> 2. admin.zocbo.com에서 시험지 번호와 시험지 데이터를 받아 extract-question을 다시 돌리는 로직 개발


- [ ] api 개발 완료




## 3. html -> 한글 문서로 변환하는 풀이팁을 확인 할 수 있게 준비.

### 개요
> html -> 한글 문서로 컨버팅하는 작업이 필요하다.
> 현재 풀이팁에서 html문서를 한글 문서로 파싱하여 데이터를 확인하는데, 변환하는 소스와 데이터를 확인할 수 있도록 준비.


### 처리내역
> html의 구조를 분석하고 데이터 구조 모델을 만드는 중.
> 


###### 이슈사항
> 동아출판사의 데이터를 변형하는데 있어 사업적 리스크가 존재.
> 동아출판사에서 저작권 계약에 대한 재논의 발화
> 이를 통해 동아출판사의 지문변형은 잠시 유보하고 YBM등 소규모 출판사의 지문 및 문항을 변경하여
> 저작권 계약에 통과되지 않을 경우 변형하여 사용할 수 있는 기반을 마련



## 4. 내신미주 속성 변경
### 개요
> 내신미주의 setDivCd 코드의 `학평변형`을 `기출변형`으로 name을 변경하여 사용요청


### 처리내역
> tms의 Name을 변경하게 되면 DB값 및 소스 로직에 Name을 검사하는 프로세스가 다수 존재하여
> 당장 tms의 Nm값을 변경하기보다 클라우드의 `학평변형`을 `기출변형`으로 표기 노출변 변경 처리.
> 추후 4월 이후에 다시 변경 처리 하는것을 목표



## 5. 단코 일괄등록 로직 변경

### 개요
> 단코 일괄등록시 서비스 Y로 처리 되고 있음
> 서비스 Y로 바로 처리되다보니 클라우드나 다른데 노출되는 현상이 있음
> 최초 등록시 서비스 N으로 처리 후 일괄 서비스 Y(신규 개발해야함) 으로 서비스 Y변경 처리 진행 예정

### 처리내역
>


- [ ] 처리완료

## 6. extract-question 에러 확인

## 개요
> extract-question의 오류건 확인
> 에러 메세지 : org.springframework.validation.BeanPropertyBindingResult: 2 errors Field error in object 'extractQuestionRequest' on field 'exPaprFilePath': rejected value []; codes [NotEmpty.extractQuestionRequest.e]
> 에러 코드 : org.springframework.validation.BindException


### 처리내역
> coruse가 A로 들어오는데 이때 String값으로 들어와서 파라미터값이 맞지 않다는 에러를 발생시킴
> G타입의 수준별 문항을 동기화 처리 하다보니 이 때 A타입이 들어와서 문제가 되었음
> 현재는 족보닷컴에서 A타입을 보내는 대신 9로 보내기 떄문에 해당현상은 발생하지 않음
>
> 하지만 추후 파라미터를 String 값으로 변경 처리가 필요함


[[업무일지]]
