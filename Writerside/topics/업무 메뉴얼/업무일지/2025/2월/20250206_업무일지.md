# 20250206 업무일지

# TMS 이슈

## 1. DB저장 오류 확인 요청

### 개요
> 수능 미주속성
> `[구분][출제년도][출제월][출처-출처소제목][형식][출제학년][페이지][문항번호][연관출처][배점][정답률][답지반응률][수학보기][과학실험][영어지문유형][국어작품명][동영상코드][음원코드][ebank문항코드]`
>
> `[정답률]`
>    - 95%, 95 이와 같이 % 입력 유무와 관계없이 저장되는 것으로 알고 있는데 %를 삭제하니 저장이 되는 것으로 보아 해당 항목 입력값 기준은 %없이 저장하는 것으로 수정하겠습니다.
>
> `[ebank문항코드]`
>    - 이투스에서 활용하기 위해 생성된 항목이나 기본값으로 저장이 되지 않고 `[0]`으로 저장하면 저장이 되고 있습니다. 해당 내용은 확인 부탁드립니다.





## 2. 단코 일괄등록 이슈

### 개요
> 미분류 -> 미분류일 때 9로 안들어가고 저장이 안됨
> 

### 처리내역
> 패턴 단원코드 입력시 쿼리에서 .equals()로 String값을 검사하지 않고 == 로 값 검사를 처리하는 코드 이슈가 발생
> 해당 부분 수정되었음.


- [x] 처리완료





## 3. AI 추출 팁 html 옛한글 깨짐 현상
### 개요
> AI로 생성한 html tip 데이터 옛한글 깨짐 현상
> 인코딩 문제인지, 폰트 문제인지 확신할 순 없으나 깨짐
> 테스트 필요



## 4. AI 수준별 자동생성 개발

### 개요
> 수준별 시험지 자동화 프로세스 개발
> 
> 목적 :: [ 자동화, 최대한 로직유지, 비생성 AI시험지 회차 자율화 ]
> 
> 1. 자동 배치화 : -> 이벤트 시작시점 :: 검색엔진 색인 시 수준별 배치 테이블 삽입 (시험지번호 생성시)
> 2. 색인 후 배치 실행 (하루한번 아침6시) :: 족보api서버에서 진행
>    배치 테이블(5,824)에서 가져가 TMS모듈실행
> 
> 3. TMS모듈에서 시험지번호와 특정유저id값으로 임의의 시험지9개를 만들고 
>    S3에 만들어진 파일을 넣고 Path값(기존 EEXA)과 결과테이블을 족보DB에 넣어준다.
> 
> ///////////////////////////////////////////////////////////////////////////////////////////
> Case "E" :
> 	Select Case Exfl_Group
> 		Case "2" : Path2 = "MEHA"
> 		Case "4" : Path2 = "MEHHWP2002"
> End Select
> ///////////////////////////////////////////////////////////////////////////////////////////
> 	- 결과 테이블 (TB_SUJUN_AI_MAKED_FILES)
> 
> SAMF_NO / exPapr_no / Make_Num / FileName / new_exPapr_no / RegDate
> FilePath를 저장할지, 대표파일의 그룹형태 Path값을 유지할지? [고민]
> 	- 새로운 exPapr_no 생성형으로 그룹코드 생성
> 
> 프로시저 생성
> : 기존 데이터는 유지하되. 
> 	그룹코드 변경 E -> G / 코어타입은 유지.
> 	파일명 변경? - 회차추가?
> 	새로생긴 new_exPapr_no 번호 추가색인
> 
> 	INSERT INTO tb_ExamPaper_Index (exPapr_no, TYPE)
> 	SELECT exPapr_no, 'IU'
> 	FROM TEMP_20191128
> 
> 
> 
> ============================ 사용안함 ==========================================
> 4. 시험지 색인값(결과테이블에서 count정리)에 몇개 시험지인지를 넣어준다.
> 
>    생성형 아이콘을 뿌리는용도로 카운트 정리
> 
> 
> 
> 대표파일 -2334, 9934, 62334, 13455
> 
> 1회차 - 대표파일 번호
> 
> 2회차 - 대표파일 번호



### 처리내역
> 1. 분당 1회 exam에서 배치 호출
> 2. select한 expaperNo를 로그 테이블에 적재
> 3. 호출된 result 값을 TMS모듈에 전달
> 4. 모듈에서 반환된 값을 받아 테이블에 insert





# 보류 및 대기건

## 1. 수능 2022년 개정 이투스 안내

### 개요
수능 2022년 개정이 시작되면서 이투스가 수능 개정이 시작되면서 이투스의 단코 입력이 필요한 상황
수능 2022개정 데이터 정리해서 김한길 담당자한테 안내

### 처리 내역
> 2022년 단코를 입력함에 따라 필요한 사전 로우 데이터
> 1. 교육과정
> 2. 학년
> 3. 과목
> 4. 출판사
> 5. 대단원
> 6. 중단원
> 7. 소단원
> 8. 내용토픽
> 9. 형식토픽

데이터를 수능 2015개정에 따라 입력 처리



## 2. 족보닷컴 어드민 시험지 변경 감지 프로세스 개발

### 개요
> 시험지 변경(오답, 수정 등)의 이유로 기존에 extract-question서비스에서 시험지 동기화가 완료된 시험지들의
> hwp model에서 한번 더 시험지 동기화 시스템 추가 개발

### 조건

> 1. 시험지가 등록된 상태에서 변경 처리(hwp 파일이 변경 되었을 때)
> 2. admin.zocbo.com에서 시험지 번호와 시험지 데이터를 받아 extract-question을 다시 돌리는 로직 개발


- [ ] api 개발 완료




## 3. html -> 한글 문서로 변환하는 프리팁을 확인 할 수 있게 준비.

### 개요
> html -> 한글 문서로 컨버팅하는 작업이 필요하다.
> 현재 풀이팁에서 html문서를 한글 문서로 파싱하여 데이터를 확인하는데, 변환하는 소스와 데이터를 확인할 수 있도록 준비.


### 처리내역
> html의 구조를 분석하고 데이터 구조 모델을 만드는 중.
> 


###### 이슈사항
> 동아출판사의 데이터를 변형하는데 있어 사업적 리스크가 존재.
> 동아출판사에서 저작권 계약에 대한 재논의 발화
> 이를 통해 동아출판사의 지문변형은 잠시 유보하고 YBM등 소규모 출판사의 지문 및 문항을 변경하여
> 저작권 계약에 통과되지 않을 경우 변형하여 사용할 수 있는 기반을 마련



## 4. 내신미주 속성 변경
### 개요
> 내신미주의 setDivCd 코드의 `학평변형`을 `기출변형`으로 name을 변경하여 사용요청


### 처리내역
> tms의 Name을 변경하게 되면 DB값 및 소스 로직에 Name을 검사하는 프로세스가 다수 존재하여
> 당장 tms의 Nm값을 변경하기보다 클라우드의 `학평변형`을 `기출변형`으로 표기 노출변 변경 처리.
> 추후 4월 이후에 다시 변경 처리 하는것을 목표



[[업무일지]]
