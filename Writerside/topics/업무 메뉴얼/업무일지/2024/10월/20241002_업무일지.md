# 빅쿼리 이슈

### 1. 중복 로직 통합 및 각 Exception별 에러 메세지 통합
##### 원인

노후화 된 모델 사용으로 인한(System.Web.mvc) 리퀘스트 파라미터 모델 바인딩 및 IIS 구동으로 인한 CI/CD 구축의 어려움이 있어 리펙터링 결정.

##### 문제 분석 및 전략

1. GETMethod, POSTMethod의 공통적인 비지니스 모델을 각 메소드마다 처리하고 있음
   - [x] 처리
2. 클라이언트가 잘못된 데이터를 보낼 경우 해당 기능을 Exception 처리하여 클라이언트에 알려주고 있지 않음.
   - [x] 처리
3. 필요하지 않은 로직을 통해 처리속도가 느려지고 있음
   - [x] 처리
4. iis 사용을 통한 배포 및 파이프라인 구축의 어려움

##### 해결방안
1. 현재 사용중인 라이브러리(System.Web.mvc) -> Microsoft.AspNetCore.Mvc 변경 처리
   - [x] 보류
2. GETMethod, POSTMethod의 로직 처리를 공통 비지니스 로직으로 통합하여 유지보수 
   - [x] 처리완료
4. Server / Client Exception을 분리하여 Client의 감지 가능하게 변경
   - [x] 처리완료
5. dev / Staging / releace 서버 구분
   - [x] 처리완료





### 2. 리펙터링 소스 dev 서버 생성
##### 1. 원인
리펙터링 소스를 테스트할 공간이 필요하게 되어 데이터 테스트 진행

- [x] 처리







# TMS 이슈사항
### 1. TMS 문항 추출(마법사)시 파일 이름 변경

##### 원인
기존 마법사에서 파일을 추출시 Tx코드 및 기초 데이터를 파일 이름으로 저장

##### 변경 사유
2022년 개정을 맞이하여 기초 데이터 및 추가 정보(개정 정보)를 파일 이름에 표기하기 위함


##### 작업 범위
현재 TMS에서 파일 이름에 TX코드 및 `"/exam/exam-text-info.asp"` 에 통신 요청을 보내는 모든 api 및 클라이언트 ~~사실상 전수조사...~~


##### 확인 내역
```mssql
[TB_GEAJUNG_YEAR]
[usp_api_TextInfo_get]
```

url : wizard/file

```java

/**
 * 과목, 교과서 한줄조회 API 호출
 * 
 * @param request
 * @return
 */
@PostMapping(value = "/exam/exam-text-info.asp",  produces = "application/json")
ExamInfoLinkApiResponse getExamTextInfo(@RequestBody ExamTextInfoApiRequest request);
```





[[업무일지]]