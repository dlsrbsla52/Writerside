

# TMS 이슈


### 1. LaTeX To HWP 문서 수식 변환

##### 내용
**tms-admin-client 처리 방식** 
> 1. 클라이언트에서 html변환 클릭
> 2. 해당하는 문제 파일을 .hml[^1]  파일(temp)로 저장
> 3. .hml 파일을 읽은 후 hwp 수식 문자열을 LaTeX 문자열로 변환
> 4. Web View에 변환한 LaTeX 문자열 노출



##### 처리 방식
**tms-admin-client 처리 방식 설계** 
> 1. Web View에 있는 Html(LaTex)[^2] 을 text로 넘김
> 2. 텍스트로 넘겨받은 데이터를 .replace() 해서 문자열 변환









# 빅쿼리 이슈

##### 1. TB_KPI_RAW_SALE 테이블 관련 이슈

```SQL
SELECT *
FROM  data.TB_KPI_RAW_SALE
WHERE PAY_DATE >= '2024-10-11 00:00:00.000000'
  AND PAY_PRODUCT LIKE "%예비중1%"
```

해당 쿼리를 실행했을 때 CATEGORY1에 해당하는 데이터 확인









# 외주관리 프로그램(OMS) 이슈

### 배포 서버 문의
```mail
안녕하세요
웨이블 개발자 신유동입니다.

OMS 1차 버전을 교육지대측 서버에 배포하기 위한 몇가지 문의사항 드립니다.

1. 배포할 ssh 접속 정보 공유 부탁드립니다.

2. 이전에 공유 주셨던 DB중 어떤 DB에 1차 버전 스키마를 적용시킬면 될지 공유 부탁드립니다.

감사합니다.
```










[^1]: HWPML(Hangul Word Processor Markup Language) 파일 형식은 한/글의 파일 형식을 Markup된 태그 구조로 나타낸 것으로, HTML과 같은 파일 형식의 Formatting Language입니다.
[^2]: **과학, 수학, 공학** 분야에서 복잡한 수식과 전문적인 문서를 작성하기 위해 널리 사용되는 조판 시스템입니다. **LaTeX 문자열**은 이러한 수식이나 문서의 내용을 텍스트 기반의 명령어로 표현한 것입니다.

[[업무일지]]